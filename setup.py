from setuptools import setup, find_packages

setup(
    name="ludex-framework",
    version="3.1.0",
    packages=find_packages(),
    install_requires=[
        # Core dependencies are in requirements.txt
    ],
    python_requires=">=3.12",
)
